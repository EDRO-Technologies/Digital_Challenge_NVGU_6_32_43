# Telegram Бот для Расписания

Telegram бот на aiogram для управления и просмотра расписания занятий.

## Возможности

### Для студентов:
- 📚 Выбор специальности
- 📅 Просмотр расписания на сегодня
- 📋 Просмотр расписания на неделю
- 🔍 Поиск по предмету или преподавателю

### Для преподавателя:
- ➕ Добавление расписания вручную
- 📤 Загрузка расписания из Excel файлов
- 📝 Редактирование и удаление записей
- 📚 Управление специальностями
- 📋 Просмотр всех расписаний

## Установка

1. Клонируйте репозиторий или скачайте файлы

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Настройте базу данных PostgreSQL:
   - Установите PostgreSQL (если еще не установлен)
   - Создайте базу данных: `CREATE DATABASE schedule_db;`
   - Создайте пользователя (опционально): `CREATE USER postgres WITH PASSWORD 'your_password';`

5. Заполните `.env` файл или настройте `config.py`:
   - `BOT_TOKEN` - токен вашего бота (получить у @BotFather)
   - `ADMIN_ID` - ваш Telegram ID (узнать у @userinfobot)
   - `POSTGRES_HOST` - хост PostgreSQL (по умолчанию: localhost)
   - `POSTGRES_PORT` - порт PostgreSQL (по умолчанию: 5432)
   - `POSTGRES_DATABASE` - имя базы данных (по умолчанию: schedule_db)
   - `POSTGRES_USER` - пользователь PostgreSQL (по умолчанию: postgres)
   - `POSTGRES_PASSWORD` - пароль PostgreSQL (по умолчанию: postgres)

6. Запустите бота:
```bash
python main.py
```

## Структура проекта

```
project/
├── database/          # Модули работы с БД
│   ├── __init__.py
│   └── db.py
├── handlers/          # Обработчики команд
│   ├── __init__.py
│   ├── start.py
│   ├── student_handlers.py
│   └── teacher_handlers.py
├── keyboards/         # Клавиатуры
│   ├── __init__.py
│   └── inline.py
├── utils/             # Утилиты
│   ├── __init__.py
│   ├── excel_parser.py
│   └── formatters.py
├── 1/                 # Папка с Excel файлами (медицинские специальности)
├── 2/                 # Папка с Excel файлами (технические специальности)
├── config.py          # Конфигурация
├── main.py            # Точка входа
├── requirements.txt   # Зависимости
└── README.md
```

## База данных

Бот использует PostgreSQL базу данных. Создаются следующие таблицы:

- `users` - пользователи бота (студенты и преподаватель)
- `specialties` - специальности
- `schedules` - расписание занятий

### Настройка PostgreSQL

1. Установите PostgreSQL:
   - macOS: `brew install postgresql@14`
   - Ubuntu/Debian: `sudo apt-get install postgresql postgresql-contrib`
   - Windows: скачайте с [официального сайта](https://www.postgresql.org/download/)

2. Запустите PostgreSQL сервис:
   - macOS: `brew services start postgresql@14`
   - Linux: `sudo systemctl start postgresql`
   - Windows: запустите через Services

3. Создайте базу данных:
```sql
CREATE DATABASE schedule_db;
```

4. Настройте переменные окружения в `.env` или `config.py`:
```env
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=schedule_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
```

## Загрузка данных из Excel

Преподаватель может загрузить расписания из Excel файлов, находящихся в папках `1/` и `2/`. Бот автоматически определит структуру файла и добавит данные в базу.

## Использование

1. Запустите бота командой `/start`
2. Студенты выбирают специальность и просматривают расписание
3. Преподаватель (с ID из конфига) имеет доступ к панели управления

## Примечания

- ID преподавателя прописан в `config.py` (переменная `TEACHER_ID`)
- Все остальные пользователи автоматически получают роль "студент"
- База данных создается автоматически при первом запуске

